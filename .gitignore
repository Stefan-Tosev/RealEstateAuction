# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# Logs
*.log

# Env
.env
.env.local

# Node / Next.js (v2 app)
node_modules/
.next/
next-env.d.ts
*.tsbuildinfo
/coverage
/test-results
/playwright-report
/prisma/generated

# Legal-pack documents. Private by definition (docs/architecture.md §5) —
# нотариален акт, tax valuations, encumbrance certificates. These must
# never enter version control, and the directory is deliberately outside
# the web root so nothing can serve them statically.
/private/
