export default function HomePage() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "4rem 2rem" }}>
      <h1>Auction House — v2</h1>
      <p>
        The public catalogue is not built yet (Phase 1). The v1 static site
        remains the live homepage in the meantime.
      </p>
    </main>
  );
}
